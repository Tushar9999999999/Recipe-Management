spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_webshop_demo
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
    initialization-mode: never
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update